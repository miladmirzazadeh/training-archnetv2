# Openings detector (ArchNetv2)
ultralytics==8.4.58
pillow
PyMuPDF>=1.24

# Wall segmentation (UNet) — optional, only needed for the wallseg track
segmentation-models-pytorch>=0.3.4
numpy
# cairosvg  # only needed if FloorPlanCAD SVGs lack sibling PNGs (render fallback)
